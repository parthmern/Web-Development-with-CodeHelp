let first = [1,2,3,4,5];
let second = [6,7,8,9,0];

//combine = adding in the end
let combineBoth = first.concat(second);

console.log(combineBoth);

//Slice = (starting index , ended index)
// ended index cannit come in slice array

let slice = combineBoth.slice(3,7);

console.log(slice);