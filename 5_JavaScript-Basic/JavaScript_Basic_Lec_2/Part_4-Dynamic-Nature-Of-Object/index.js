let obj = {

    l : 1,
    b : 2 
};

obj.c = 4; // created new KEY in OBJ

delete obj.c ; // Deleting the newly key that was created

delete obj.a  ; // Deleting the A named key that was already presented in the object